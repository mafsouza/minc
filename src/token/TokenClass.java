package token;

/**
 * TokenClass é uma enumeração para classificar os tokens
 * 
 * @author Marco AFS
 *
 */
public enum TokenClass {
    EOF, DIGIT, SUM_OPERATOR, MULT_OPERATOR, LPAREN, RPAREN, OTHER;
}
